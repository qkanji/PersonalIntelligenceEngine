# OneNote RAG System - Requirements
# Phase 1: OneNote Extraction
pywin32>=306

# Phase 2: PDF to Markdown (Kaggle / Qwen2-VL)
pypdfium2>=4.0.0
torch>=2.0.0

# Phase 3: Embed + Upload to Pinecone
sentence-transformers>=3.0.0
pinecone>=3.0.0

# Utilities
Pillow>=10.0.0
tqdm>=4.66.0
gcloud-aio-storage
aiohttp
